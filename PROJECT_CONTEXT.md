# 投资研究项目

## 项目目标

建立一个长期、可复盘的投资研究资料库，覆盖 A 股与加密资产两个模块。

## 使用原则

- 区分事实、假设、判断与风险。
- 所有重要结论记录来源和日期。
- 先研究，后决策；先定义退出条件，再考虑建仓。
- 不在仓库中保存 API Key、交易所密钥或其他敏感信息。

## 模块

- `A-share/`：A 股公司、行业、财报、估值与政策研究。
- `crypto/`：加密资产、协议、链上数据、代币机制与风险研究。

## 当前执行范围

- A 股暂不进行自动化下单，采用每日推荐、评分、观察、人工买入和盈亏记录流程。
- 加密资产可逐步研究自动化交易，但必须先完成模拟交易、风控和人工熔断验证。
- “逐步盈利”是待验证目标，不是保证；所有模型必须记录亏损、最大回撤和失效条件。

## 初步模型验证方法

初期采用多模型并行和模拟交易方式：使用同一批历史数据和同一时间点生成信号，分别记录各模型的推荐、评分、仓位、盈亏和回撤；经过样本外验证和模拟观察期后，再整合表现稳定且逻辑互补的模型。不得只根据历史收益挑选模型，也不得因为短期亏损随意修改规则。

## 更新规则

重要研究判断写入 `DECISIONS.md`，信号和模拟执行写入 `records/`。真实成交经用户确认写入独立私有流水，再计算私有持仓视图；公开 `PORTFOLIO.md` 只保留空模板及核算口径。

## 分析前置检查

任何分析、计算或文件操作开始前，必须先检查：

1. `PORTFOLIO.md` 中的当前持仓与现金状态。
2. `DECISIONS.md` 中最近的投资判断、失效条件与复查日期。
3. `TODO.md` 中的待办事项。
4. 对应模块的最新观察名单、研究文件、数据日期和数据来源。
5. 最近的买卖、调仓、转账或其他相关操作记录。

数据过期、来源不明、缺失或冲突时，暂停依赖该信息的结论，不自行补全。缺真实持仓不阻止通用研究和独立模拟；缺关键行情停止对应评分和成交模拟。

## 建设状态与入口

2026-09-13：已建立流程和空模板；未接入验证过的行情、部署模型或定时任务，暂无真实持仓、模拟成交或验证收益记录。

从 README.md 开始。OPERATIONS.md 和 records/ 定义记录链；DATA_SOURCES.md 定义来源；models/ 和 SIMULATION_RULES.md 定义验证；WORKFLOW.md、MOBILE_INSTRUCTIONS.md 和 daily/ 定义发布及移动使用。

先定义三个逻辑不同的候选模型，初始数量不代表有效性。每日记录，按预定验证计划评估；改规则建立新版本。公开研究与私人交易信息分开保存，私有访问未建立前不要求公开上传真实持仓。

# 报告保存与发布流程

当前仅有规范，尚无运行中的调度或自动回写服务。

## 单次工作

1. 读取项目文件、对应模型和市场资料；记录 context_commit（分析使用的仓库版本）、读取时间、数据截止、成功和失败清单。
2. 检查数据，建立稳定 run_id；模型和输入未冻结则 blocked_config，关键行情缺失则 blocked_data。仍可写限制说明，不编造评分和结果。
3. 保存信号、观察名单变化、模拟订单/成交和净值；沿用 OPERATIONS.md 的编号。用户真实成交核对后另入私有流水。
4. 生成 daily/YYYY-MM-DD/市场-报告类型.md，保留输入快照引用和版本；已有历史报告用修订记录追加，不无痕替换。
5. 核对数据时间顺序、费用、现金、持仓、状态和隐私后保存。若发现部分写入，标 incomplete，修复和去重后才发布。
6. 在授权范围内提交、推送；从远程读取 main 提交号，并核对本地 HEAD 和报告文件。成功后才能向手机汇报已发布。
7. 手机端收到仓库中的报告链接及 publish_commit；仓库读取失败明确报告，不凭历史聊天补全。

context_commit 是分析前版本；publish_commit 是报告保存后的版本，两者可以不同。不要把 publish_commit 写入它自身所在的同一提交；在发布消息或下一条运行记录中记载。

## 状态与重试

- 状态：planned → running → saved → published；异常：blocked_data、blocked_config、incomplete、failed、missed。
- saved 不等于 published，候选信号不等于成交。
- 重试同一 run_id，增加 attempt，不重复创建同一 signal_id / order_id / fill_id。更正使用单独编号。
- 上传失败保留本地文件和错误摘要，重新核对远程后再重试。远程冲突需先检查并保留双方内容，不强推覆盖。
- 漏跑和离线标 missed。事后补跑标 backfill 和实际运行时间；缺历史快照时不得声称重现了当时的判断。
- 交易日历、计划时间、加密日界线、执行环境、失败通知渠道待用户确认并部署。无变化是否通知由任务设置决定，不把它当完成记录。

## 责任

- 执行端：获取数据、计算、保存、核对、发布。
- 手机端：读报告、讨论、提供用户实际执行信息；工具不支持写入时生成“待录入”内容，不宣称写回。
- 用户：确认真实交易信息。讨论与买卖建议不直接计入流水。
- 先人工跑通一次，再自动化。一个成功报告不代表定时任务可靠。
